import React from 'react';
import ReactDOM from 'react-dom/client';
// import App from './App';   // comment out for testing

ReactDOM.createRoot(document.getElementById('root')!).render(
  <div style={{ color: 'white', fontSize: 40, textAlign: 'center', marginTop: 100 }}>
    🚀 React is alive!
  </div>,
);